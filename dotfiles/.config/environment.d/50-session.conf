# Session-wide environment, applied by systemd to the entire user session.
# Covers display-manager-launched sessions and systemd user services,
# which .zshenv cannot reach.
QT_QPA_PLATFORMTHEME=qt5ct:qt6ct
NO_AT_BRIDGE=1
